// Development environment — fill in your Supabase credentials locally.
// Do NOT commit actual credentials. Keep empty values here when committing.
// See .env.example for variable names reference.
export const environment = {
  production: false,
  supabaseUrl: '',
  supabaseAnonKey: '',
};
