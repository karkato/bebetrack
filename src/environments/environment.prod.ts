// Production environment — values injected at build time via CI/CD.
// See .env.example for variable names reference.
export const environment = {
  production: true,
  supabaseUrl: '',
  supabaseAnonKey: '',
};
