# Supabase — get these from your Supabase project settings
# SUPABASE_ANON_KEY is the public anon key (safe to expose client-side, protected by RLS)
# Never commit the service_role key
SUPABASE_URL=
SUPABASE_ANON_KEY=
