# Test user credentials for npm run test:rls
# Supabase URL and anon key are read from src/environments/environment.ts
# Create these two users in Supabase Auth dashboard (Authentication > Users)
USER_A_EMAIL=parent-a@example.com
USER_A_PASSWORD=changeme
USER_B_EMAIL=parent-b@example.com
USER_B_PASSWORD=changeme
